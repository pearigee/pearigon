(ns e2e.utils.elements
  (:require [etaoin.api :as eta]))

(def sidebar {:css ".sidebar"})
(def sidebar-header {:css ".sidebar .sidebar-header"})
(def search-input {:css ".search-overlay .input"})
(def svg-root {:id :svg-root})
(def selected-search-result {:css ".search-result.selected"})
